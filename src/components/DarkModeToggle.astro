---
// src/components/DarkModeToggle.astro
---

<button style="text-align: right;" id="theme-toggle" onclick="window.toggleDarkMode()">
  <span class="dark ">☀️</span>
  <span class="light">🌑</span>
</button>

<style>
  button {
        background: none;
        border: none;
        cursor: pointer;
        font-size: 1.5rem;
        z-index: 1000;
    }

    /* ☀️ Estado por defecto (Light Mode): Muestra el Sol, Oculta la Luna */
    .light {
        display: inline;
    }
    .dark {
        display: none;
    }

    /* 🌙 Estado Dark Mode: Oculta el Sol, Muestra la Luna */
    /* Asegúrate de que tu JavaScript añada la clase '.dark-mode' al <body> o <html> */
    :global(.dark-mode) .light { 
        display: none; /* En modo oscuro, oculta el Sol */
    }
    :global(.dark-mode) .dark {
        display: inline; /* En modo oscuro, muestra la Luna */
    }
</style>